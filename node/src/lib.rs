// The node is a P2P reactor, RPC reactor, worker thread pool and a broker

use reactor::Reactor;

pub enum Error {}

pub fn run_node() -> Result<(), Error> {
    // let p2p = Reactor::new();

    unreachable!("node never returns a result");
}
